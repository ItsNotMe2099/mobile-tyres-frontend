query textPage($slug: String!, $key: String!){
    textPageBySlugOrKey(slug: $slug, key: $key){
        id,
        summary,
        body,
        slug,
        key,
        seoTitle,
        seoDescription,
        seoKeywords,
        collection {
            id,
            name
        }
    }
}
